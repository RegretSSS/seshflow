interface:
  display_name: "Seshflow Light"
  short_description: "Low-context seshflow workflow with init/init apifirst + ncfr-first progressive command reveal."
  default_prompt: "Use $seshflow-light to bootstrap this repo with init or init apifirst plus ncfr-first flow, then reveal only the next intent-matched seshflow command."

policy:
  allow_implicit_invocation: true
